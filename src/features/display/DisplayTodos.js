import TodosList from '../todos/TodosList';

const DisplayTodos = () => {
    return (
        <>
            <h1>Todo's</h1>
            <TodosList />
        </>
    );
};

export default DisplayTodos;
